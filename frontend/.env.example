# Copy to .env.local for local dev. In Vercel, set this in the Project Settings
# env vars. Leave empty (or "/api") when frontend and backend are deployed
# together on Vercel, since /api is same-origin.
VITE_API_URL=/api
